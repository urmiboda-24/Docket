import Header from "@/Components/Header";
import { useState } from "react";
import { ChromePicker, PhotoshopPicker, SketchPicker } from "react-color";
const HomePage = () => {
  return (
    <>
      <Header component={<div>Hello</div>} />
    </>
  );
};

export default HomePage;
